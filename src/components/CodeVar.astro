---
import CopyText from "./react/CopyText";

const { copy } = Astro.props;

const slotContent = await Astro.slots.render("default");
const textToCopy = copy ?? slotContent?.trim();
---

<CopyText
   text={textToCopy}
   client:load
>
   <slot />
</CopyText>
