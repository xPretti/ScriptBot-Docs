---
const { name, aliases } = Astro.props;
---

{
  aliases && (
    <p>
      <span>Ao invés de </span>
      <code>{name}[...]</code>
      <span>, vocé pode utilizar o nome encurtado </span>
      <code>{aliases}[...]</code>
    </p>
  )
}

<!-- Estilos -->
<style>
  div {
    margin-top: 2em;
    border-radius: 10px;
  }
  ul {
    display: block;
    list-style-type: disc;
    margin-block-start: 0.5em;
    margin-block-end: 0;
    padding-inline-start: 0;
  }

  code {
    border-radius: 5px;
  }
</style>
