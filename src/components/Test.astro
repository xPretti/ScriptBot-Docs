---
import { FUNCTION_MAPPING } from "@src/data/placeholder-data";

import Clipboard from "./ui/Clipboard.astro";
import PlaceholderTable from "./placeholders/PlaceholderTable.astro";

const elements = FUNCTION_MAPPING.get("candle") ? Array.from(FUNCTION_MAPPING.get("candle")!.values()) : [];
const element = elements[0];
---

<Clipboard text="test">VALOR COM HOVER</Clipboard>

<PlaceholderTable
   type="function"
   elements={elements}
/>
