---
import { PlaceholderLoader } from "./react/placeholders/PlaceholderLoader";

const { type, category } = Astro.props;
---

<PlaceholderLoader
   client:visible
   type={type}
   category={category}
/>
